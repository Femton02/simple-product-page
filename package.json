{
  "name": "simple-product-website",
  "version": "1.0.0",
  "type": "module",
  "description": "A simple product website that displays a list of products and allows the user to add products and delete them.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development nodemon Backend/server.js",
    "build": "npm install && npm install --prefix Frontend && npm run build --prefix Frontend",
    "start": "cross-env NODE_ENV=production node Backend/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.8.0",
    "nodemon": "^3.1.7"
  }
}
